# Data processing
pandas>=2.0,<4.0
numpy>=1.24,<3.0
pyarrow>=23.0.1,<25.0

# Machine learning (for linear regression)
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0

# GeoIP resolution (offline, using MaxMind GeoLite2 databases)
geoip2>=5.0,<6.0

# Retry logic
tenacity>=8.2,<10.0

# Airport data (for IATA geolocation)
airportsdata>=20230101

# Redpanda / Kafka consumer
confluent-kafka>=2.3,<3.0

# GraphQL HTTP calls for Graph Network subgraph stake data
requests>=2.33.0,<3.0

# Protobuf runtime for gateway_queries message parsing
protobuf>=5.0,<7.0

# CIDv0 base58 encoding for deployment hashes
base58>=2.1,<3.0

# Async HTTP for /dips/info endpoint fetching
aiohttp>=3.13.4,<4.0

# PEP 440 version comparison for the graph-node minimum-version filter
packaging>=21.0,<26.0

# Transitive pins to keep the vulnerability scanner happy.
idna>=3.15
urllib3>=2.7.0
