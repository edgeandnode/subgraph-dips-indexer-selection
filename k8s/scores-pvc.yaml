apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: iisa-scores
  namespace: subgraph-dips-indexer-selection-iisa-daily-cronjob-samuel
  labels:
    app: iisa
spec:
  storageClassName: standard-rwx
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
